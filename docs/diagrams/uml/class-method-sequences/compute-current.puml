@startuml
title BIỂU ĐỒ TUẦN TỰ — compute_current
skinparam backgroundColor white
skinparam defaultFontName Arial
skinparam defaultFontSize 24
skinparam dpi 300
skinparam shadowing false
skinparam sequence {
  ArrowColor #345B52
  LifeLineBorderColor #299D78
  ParticipantBorderColor #299D78
  ParticipantBackgroundColor #F4FBF8
}
actor "Người sử dụng" as User
boundary "Màn hình Mục tiêu tiết kiệm" as UI
control "Backend\n(Router + Core)" as Backend
database "CSDL" as DB
User -> UI: Chọn compute_current
UI -> Backend: Gửi yêu cầu compute_current
activate Backend
Backend -> Backend: SUM các bản ghi deposit
Backend -> Backend: SUM các bản ghi withdraw
Backend -> Backend: Trả deposit−withdraw
alt Xử lý thành công
  Backend --> UI: Kết quả không được lưu thành\ncột và phản ánh toàn bộ lịch\nsử.
  UI --> User: Hiển thị kết quả
else Dữ liệu không hợp lệ hoặc lỗi xử lý
  Backend --> UI: Mã lỗi và thông báo phù hợp
  UI --> User: Hiển thị lỗi, giữ dữ liệu cần thiết
end
deactivate Backend
@enduml
